<h1>🍕 <%= @pizza.name %> is yummy! 🍕 </h1>

<h4>Ingredients</h4>
<ul>
  <li>
    <%= @pizza.ingredients %>
  </li>
</ul>
<br>
<%= button_to "Back to Restaurants", restaurants_path, method: :get %>
<br>
<%= button_to "Edit?", edit_pizza_path(@pizza), method: :get %>
<br>
<%= button_to "Delete Pizza", @pizza, method: :delete, data: {confirm: "(Θ︹Θ)ს are you sure?"} %>
